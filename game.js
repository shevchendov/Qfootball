/**
 * =====================================================================
 *  小游戏入口（game.js）
 * =====================================================================
 *  1. 初始化微信云开发（必须在调用云函数/watch 前完成）。
 *  2. bootstrap.init()：一次性初始化画布/渲染/输入/主循环（大厅态）。
 *  3. matchManager.start('random')：断线重连分派或进入大厅，
 *     点击「开始比赛」后匹配，成功后内部调用 bootstrap.joinGame() 进入对局。
 * =====================================================================
 */

// 初始化微信云开发
wx.cloud.init({
  env: 'cloud1-d7g9uo3la0e6ce2c4', // 云开发环境 ID
  traceUser: true,
});

// 一次性初始化（渲染空闲场景 + 主循环）
require('./core/bootstrap').init();

// 发起随机匹配（成功后自动入房）
require('./core/matchManager').start('random');
